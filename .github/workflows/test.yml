name: Test Action

on:
  push:
    branches:
      - main

jobs:
  typical-workflow:
    name: Test
    uses: ./.github/workflows/pnpm-typical.yml
  typical-workflow-use-engine:
    name: Test with use-engine
    uses: ./.github/workflows/pnpm-typical.yml
    with:
      use-engines: true
